syntax = "proto3";

service WalletService {
    rpc Buy(Order) returns (OrderResponse) {}
}

message Order {
    string userId = 1;
    float price = 2;
}

message OrderResponse {
    string userId = 1;
    bool success = 2;
    string error = 3;
}